# Docker
# Build and push an image to Azure Container Registry
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- main

resources:
- repo: self

variables:
  # Container registry service connection established during pipeline creation
  dockerRegistryServiceConnection: '1f9ccfae-f409-4332-a67e-31f939dc6448'
  imageRepository: 'crystalgaobloodpressure'
  containerRegistry: 'aksdevopsacrbp1353.azurecr.io'
  dockerfilePath: '$(Build.SourcesDirectory)/BE/Dockerfile'
  tag: '$(Build.BuildId)'

  # Agent VM image name
  vmImageName: 'ubuntu-latest'

stages:
- stage: BuildBE
  displayName: Build BE
  jobs:
  - job: Build_WAR
    displayName: Build WAR file
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: Maven@4
      displayName: 'Build WAR file'
      inputs:
        mavenPomFile: '$(Build.SourcesDirectory)/BE/pom.xml'
        goals: 'clean package'
        publishJUnitResults: true
        testResultsFiles: '**/surefire-reports/TEST-*.xml'
        javaHomeOption: 'JDKVersion'
        mavenVersionOption: 'Default'
        mavenAuthenticateFeed: false
        effectivePomSkip: false
        sonarQubeRunAnalysis: false
        checkStyleRunAnalysis: true
        pmdRunAnalysis: true
        findBugsRunAnalysis: true
    - task: Docker@2
      displayName: Build and push an image to container registry
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        containerRegistry: $(dockerRegistryServiceConnection)
        tags: |
          $(tag)
